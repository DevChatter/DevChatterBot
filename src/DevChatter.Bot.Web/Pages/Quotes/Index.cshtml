@page
@model DevChatter.Bot.Web.Pages.Quotes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QuoteEntity[0].QuoteId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuoteEntity[0].Text)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuoteEntity[0].Author)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuoteEntity[0].AddedBy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuoteEntity[0].DateAdded)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.QuoteEntity) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QuoteId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Text)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Author)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AddedBy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateAdded)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
